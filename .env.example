# The same-origin route is proxied to the public aggregate repository.
# Override only when testing another compatible dataset.
NEXT_PUBLIC_DATA_BASE_URL=/data

# Canonical production URL used for metadata and social previews.
NEXT_PUBLIC_SITE_URL=https://arrival-atlas.vercel.app
